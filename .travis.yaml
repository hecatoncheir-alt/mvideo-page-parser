language: go
go:
  - "1.11"
  - master

before_install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh

install: true

before_script:
  - dep ensure

script:
  - go test ./...
